services:
  - type: web
    name: movierecchatbot-backend
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: QWEN_API_KEY
        sync: false
      - key: NODE_ENV
        value: production 